import React from 'react'
import Layout from '../components/layout'

function Blog(props) {

    return (
        <Layout location={props.location}>
            
        </Layout>
    )
}

export default Blog;